apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: acmehub-syncer-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: acmehub-syncer-role
subjects:
  - kind: ServiceAccount
    name: acmehub-syncer
    namespace: acmehub-system
